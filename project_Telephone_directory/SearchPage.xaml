<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:project_Telephone_directory.Controls"
             x:Class="project_Telephone_directory.SearchPage"
             Title="Поиск">
    <Grid>
        <controls:ShaderBackgroundView x:Name="ShaderBg"
                                         TypingIntensity="{Binding TypingIntensity}" />
        <Grid RowDefinitions="Auto,Auto,*" Padding="12" RowSpacing="10">
            <Border Grid.Row="0" BackgroundColor="#55000000" StrokeThickness="0" Padding="6">
                <WebView x:Name="SketchfabSearch" HeightRequest="140" />
            </Border>
            <Entry Grid.Row="1"
                   Placeholder="Введите имя, телефон или email"
                   Text="{Binding Query}"
                   TextColor="White"
                   PlaceholderColor="#AAFFFFFF" />
            <CollectionView Grid.Row="2" ItemsSource="{Binding Results}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8" ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Column="0" Spacing="2">
                                <Label Text="{Binding Name}" FontSize="18" TextColor="White" />
                                <Label Text="{Binding Phone}" FontSize="14" TextColor="#DDFFFFFF" />
                            </VerticalStackLayout>
                            <Button Grid.Column="1"
                                    Text="Открыть"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenDetailCommand}"
                                    CommandParameter="{Binding .}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>
